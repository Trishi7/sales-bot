# Discord gateway client.
discord.py>=2.4.0
# The bot's own model calls: question answering, commitment detection, nudge wording.
anthropic>=0.39.0
# Transport used by the Anthropic SDK; pinned here so a resolver can't pick an
# incompatible one behind our back.
httpx>=0.27.0
# Loads .env in config.py.
python-dotenv>=1.0.0

# --- GTM Playbook, via the Google Sheets API ---------------------------------
# The sheet is read LIVE through the API with a service account (gtm_sheet.py) —
# it is never exported, downloaded, or synced to disk. rclone is used ONLY for
# the meeting-notes docs and has nothing to do with the spreadsheet.
gspread>=6.0.0
google-auth>=2.28.0
#
# NOT required, deliberately:
#   openpyxl — nothing reads .xlsx. The sheet is read over the API, so there is
#              no workbook file to parse. Don't add it back without a reader
#              that actually needs it.
